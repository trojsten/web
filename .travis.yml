language: python
python:
  - "2.7"
env:
  - DJANGO_SETTINGS_MODULE=trojsten.settings.production
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - ./manage.py test
sudo: false
